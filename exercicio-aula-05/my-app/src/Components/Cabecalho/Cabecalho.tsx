import { CabecalhoProps } from "../../types"

export default function Cabecalho ({paginaprops}: CabecalhoProps){

    document.title = paginaprops;
    
    return (
        <header>
            
        </header>

    )
}